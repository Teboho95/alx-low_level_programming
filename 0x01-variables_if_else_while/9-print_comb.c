#include<stdio.h>
/**
 * main-possible combinations of single digit numbers
 *
 * Return: 0 (Success)
 */
int main(void)
{
	int n;

	for (n = 48; i < 58; i++)
	{
		putchar(n);
		if (n != 57)
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n');
	return (0);
}
